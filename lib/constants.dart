import "package:flutter/material.dart" ;

const primaryBckgnd = Color(0xff0E1434);
const secondaryBckgnd = Color(0xff262B48);
const textColor = Color(0xff000000);
const whiteColor = Colors.white;
const greyColor = Color(0xffF5F6FB);
const greycolor2 = Colors.grey;
const iconColor = Color(0xff5F97F4);

const primaryBckgndLight = Color(0xffE9EEF4);

class AppMargins {
  static const EdgeInsets small = EdgeInsets.only(top: 8, left: 8, right: 8);
  static const EdgeInsets medium =
      EdgeInsets.only(top: 16, left: 16, right: 16);
  static const EdgeInsets large = EdgeInsets.only(top: 32, left: 32, right: 32);
}
